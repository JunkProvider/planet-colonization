<UserControl x:Class="SpaceLogistic.WpfView.View.CelestialBodyIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:SpaceLogistic.WpfView.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewModel:CelestialBodyViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Canvas>
        <Ellipse DockPanel.Dock="Left" Width="{Binding IconSize}" Height="{Binding IconSize}" Margin="5 0 15 0" Fill="SteelBlue" />
    </Canvas>
</UserControl>
